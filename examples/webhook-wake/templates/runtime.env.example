# Copy to /etc/webhook-wake/runtime.env (mode 0640). No secrets or API keys.
# System units cannot use %h/%U for the service User= (those are the manager).
HOME=/home/example-operator
XDG_RUNTIME_DIR=/run/user/1000
USER=example-operator
LOGNAME=example-operator
