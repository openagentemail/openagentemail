# Copy to /etc/webhook-wake/runtime.env (mode 0640). No secrets or API keys.
# Must match webhook-wake.service User=ops. System %h/%U is the manager (root).
HOME=/home/ops
XDG_RUNTIME_DIR=/run/user/1000
USER=ops
LOGNAME=ops
